services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: veloroute
      POSTGRES_USER: veloroute
      POSTGRES_PASSWORD: veloroute
    ports:
      - "5432:5432"
    volumes:
      - veloroute_pgdata:/var/lib/postgresql

volumes:
  veloroute_pgdata:
